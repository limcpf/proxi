# 핵심 원칙

상태: `accepted`

## 원칙
- `AGENTS.md` 는 라우터로 유지하고 세부 지식 저장소로 비대화하지 않는다.
- 저장소 안에 없는 규칙은 팀의 공식 기준으로 취급하지 않는다.
- 구조 변경은 이유와 경계를 문서로 남긴다.
- 실행 기억은 대화가 아니라 버전 관리되는 문서에 남긴다.
- 자동 검증이 없는 규칙은 임시 규칙으로 간주하고 공백을 드러낸다.
