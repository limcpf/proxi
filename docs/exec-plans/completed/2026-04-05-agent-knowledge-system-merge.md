# 에이전트 문서 운영 시스템 병합

상태: `completed`
완료일: `2026-04-05`

## 목표
- 기존 모노레포에 라우터형 `AGENTS.md` 와 `docs/` 기반 지식 저장소 구조를 병합한다.

## 범위
- 루트 `AGENTS.md`, `ARCHITECTURE.md` 정리
- `apps/back/AGENTS.md` 작성
- `docs/` 초기 구조와 운영 문서 추가

## 작업 단계
- 기존 루트/백엔드 문서를 확인했다.
- 라우터, 설계 문서, 실행 계획, 품질 문서의 책임을 분리했다.
- 현재 저장소 상태를 기준으로 초기 품질 점수와 기술 부채를 기록했다.

## 검증 방법
- 경로와 링크가 현재 저장소 구조와 맞는지 수동 점검했다.
- 자동 `test` 나 `lint` 명령은 아직 정의되어 있지 않아 실행하지 못했다.

## 결정 로그
- 존재하지 않는 `front`/`shared` 프로젝트 디렉터리는 생성하지 않았다.
- 현재 저장소에 있는 `apps/back` 만 프로젝트별 라우터를 추가했다.
- 문서 자동화는 즉시 도입하지 않고 기술 부채로 남겼다.

## 남은 이슈
- 문서 최신성 검사 자동화가 없다.
- 백엔드 실행/검증 명령이 아직 정의되어 있지 않다.
